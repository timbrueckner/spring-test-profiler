```bash
# Install Marp CLI if needed
npm install -g @marp-team/marp-cli

# Generate PDF from a slide deck
marp --pdf index.md --theme pragmatech.css --allow-local-files

# Start the Marp Server
marp project-pitch --theme project-pitch/pragmatech.css --server
```
